node_modules/
dist/
*.log
*.tsbuildinfo

# Generated by `npm run notes` at deploy time. A build artifact, not a source
# file — absent in dev, where the chip simply does not render.
public/patch-notes.json

# throwaway harness pages used for screenshots (level viewer, fx test bench)
_shots/
_fx/

# headless verification scratch (esbuild bundles for node probes)
.probe*
.*check.*.mjs
